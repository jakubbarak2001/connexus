---
import Container from "./Container.astro";
import SectionMarker from "./SectionMarker.astro";

const cards = [
  {
    index: "01",
    headline: "Web",
    body: "Vlastní design, vlastní texty, SEO základem a analytika. Žádná šablona, žádná plichta s kolegy z oboru.",
  },
  {
    index: "02",
    headline: "Růst",
    body: "Po spuštění optimalizace na datech, péče o Google profil, drobné úpravy webu. Cílem je, aby vás Google začal víc nacházet, a vy věděli proč.",
  },
  {
    index: "03",
    headline: "Vy",
    body: "Žádné balíčky, žádná smlouva na rok, žádný výpovědní rituál. Péči po spuštění si vyberete sami, nebo žádnou.",
  },
];
---

<section id="co-delam" class="bg-bg-secondary py-20 md:py-32">
  <Container>
    <SectionMarker number="02" label="Co dělám" />

    <h2
      class="mt-10 max-w-[900px] font-serif text-h2-sm font-medium leading-[1.05] md:mt-14 md:text-h2"
    >
      Web. Růst. <em class="not-italic text-accent">Vy</em>.
    </h2>

    <p class="mt-8 max-w-prose text-body-lg text-text-secondary md:mt-10">
      Tři vrstvy, žádné balíčky. Vlastní web.
    </p>

    <div
      class="mt-14 grid grid-cols-1 gap-10 md:mt-20 md:grid-cols-3 md:gap-12"
    >
      {
        cards.map((card) => (
          <div class="services-card border-t border-border-strong pt-8">
            <p class="font-mono text-small text-accent">{card.index}</p>
            <h3 class="mt-3 font-serif text-h3-sm font-medium text-text-primary md:text-h3">
              {card.headline}
            </h3>
            <p class="mt-4 text-body text-text-secondary">{card.body}</p>
          </div>
        ))
      }
    </div>

    <p class="mt-14 max-w-prose text-body text-text-secondary md:mt-20">
      Cena se odvíjí od konkrétního zadání. Rád ji s vámi proberu na úvodním
      <a
        href="#kontakt"
        class="text-text-primary underline decoration-border-strong decoration-[1.5px] underline-offset-[6px] transition-colors hover:decoration-accent hover:text-accent"
      >30minutovém hovoru</a> — zdarma a bez závazku.
    </p>
  </Container>
</section>

<style>
  .services-card {
    opacity: 0;
    transform: translateY(16px);
    transition:
      opacity 700ms cubic-bezier(0.16, 1, 0.3, 1),
      transform 700ms cubic-bezier(0.16, 1, 0.3, 1);
  }
  .services-card.is-visible {
    opacity: 1;
    transform: none;
  }
  @media (min-width: 768px) {
    .services-card:nth-child(2) {
      transition-delay: 120ms;
    }
    .services-card:nth-child(3) {
      transition-delay: 240ms;
    }
  }
  @media (prefers-reduced-motion: reduce) {
    .services-card {
      opacity: 1;
      transform: none;
      transition: none;
    }
  }
</style>

<script>
  const cards = document.querySelectorAll<HTMLElement>(
    "#co-delam .services-card",
  );
  if (cards.length) {
    const prefersReduced = window.matchMedia(
      "(prefers-reduced-motion: reduce)",
    ).matches;

    if (prefersReduced) {
      cards.forEach((card) => card.classList.add("is-visible"));
    } else {
      const observer = new IntersectionObserver(
        (entries, obs) => {
          entries.forEach((entry) => {
            if (entry.isIntersecting) {
              entry.target.classList.add("is-visible");
              obs.unobserve(entry.target);
            }
          });
        },
        { threshold: 0.25, rootMargin: "0px 0px -15% 0px" },
      );
      cards.forEach((card) => observer.observe(card));
    }
  }
</script>
