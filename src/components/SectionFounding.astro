---
import Container from "./Container.astro";
import SectionMarker from "./SectionMarker.astro";
import Button from "./Button.astro";

interface Slot {
  number: string;
  status: "obsazeno" | "volné";
  name?: string;
  meta?: string;
}

const FOUNDING_PRICE_DISPLAY = "35 000";
const STANDARD_PRICE_DISPLAY = "60 000";

const slots: Slot[] = [
  {
    number: "01",
    status: "obsazeno",
    name: "Markéta Baráková",
    meta: "Pojišťovací poradkyně · uzavřeno 03/2026",
  },
  {
    number: "02",
    status: "volné",
  },
  {
    number: "03",
    status: "volné",
  },
];
---

<section id="zakladajici" class="bg-bg-tertiary py-20 md:py-32">
  <Container>
    <SectionMarker number="03" label="Zakládající klienti" />

    <div class="mt-10 max-w-[900px] md:mt-14">
      <h2 class="font-serif text-h2-sm font-medium leading-[1.05] md:text-h2">
        Tři základající klienti.<br />
        <span class="text-accent">Zbývají dvě místa.</span>
      </h2>
    </div>

    <p
      class="mt-10 font-mono text-small uppercase tracking-[0.08em] text-text-muted md:mt-14"
    >
      Zaváděcí cena · platná do naplnění tří míst
    </p>

    <ol class="mt-6 border-t border-border-strong">
      {
        slots.map((slot) => (
          <li class="border-b border-border-strong py-6 md:grid md:grid-cols-12 md:items-baseline md:gap-8 md:py-8">
            <span class="block font-mono text-small text-text-muted md:col-span-1">
              {slot.number}
            </span>

            {slot.status === "obsazeno" ? (
              <Fragment>
                <div class="mt-2 md:col-span-8 md:mt-0">
                  <p class="font-serif text-h3-sm font-medium text-text-primary md:text-h3">
                    {slot.name}
                  </p>
                  <p class="mt-1 font-mono text-small text-text-muted">
                    {slot.meta}
                  </p>
                </div>
                <span class="mt-3 inline-block font-mono text-small uppercase tracking-[0.08em] text-accent md:col-span-3 md:mt-0 md:justify-self-end">
                  ✓ Obsazeno
                </span>
              </Fragment>
            ) : (
              <Fragment>
                <div class="mt-2 md:col-span-8 md:mt-0">
                  <p class="font-serif text-h3-sm font-medium text-text-muted md:text-h3">
                    — Volné místo —
                  </p>
                  <p class="mt-1 font-mono text-small text-text-muted">
                    K dispozici pro zakládajícího klienta
                  </p>
                </div>
                <div class="mt-3 flex items-baseline gap-3 md:col-span-3 md:mt-0 md:flex-col md:items-end md:gap-1 md:text-right">
                  <p class="font-serif text-h3-sm font-medium text-text-primary md:text-h3">
                    {FOUNDING_PRICE_DISPLAY} Kč
                  </p>
                  <p class="font-mono text-small text-text-muted line-through">
                    {STANDARD_PRICE_DISPLAY} Kč
                  </p>
                </div>
              </Fragment>
            )}
          </li>
        ))
      }
    </ol>

    <div class="mt-12 grid gap-10 md:mt-16 md:grid-cols-12 md:gap-16">
      <div class="md:col-span-7">
        <div class="max-w-prose space-y-6 text-body text-text-secondary">
          <p>
            Beru si v tomto roce tři zakládající klienty. Vybudovat případové
            studie pořádně chce čas a pozornost, kterou nemůžu rozdělit mezi víc
            lidí. Pokud jeden z těch tří budete vy, dostanete za sníženou cenu
            šest měsíců péče v základu a prioritní dostupnost.
          </p>
          <p>
            <strong>Není to sleva. Je to dohoda:</strong> vy mi dáte šanci postavit
            portfolio na reálných datech, já vám dám pozornost, kterou s plnou klientelou
            později nemůžu nabídnout.
          </p>
        </div>
      </div>

      <div class="md:col-span-5">
        <p class="font-mono text-small uppercase tracking-[0.08em] text-text-muted">
          Co dostanete
        </p>
        <ul class="mt-4 space-y-2 text-body text-text-secondary">
          <li>Šest měsíců péče po spuštění</li>
          <li>Prioritní dostupnost a přímý kontakt</li>
          <li>Strategický check-in v měsíci 4</li>
          <li>Závěrečná revize v měsíci 6</li>
        </ul>
      </div>
    </div>

    <div class="mt-12 md:mt-16">
      <Button href="#kontakt" variant="primary">Domluvit konzultaci</Button>
    </div>
  </Container>
</section>
